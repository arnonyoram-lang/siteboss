# הפעלת 3 המתגים ב-Firebase — מדריך צעד-אחרי-צעד (יורם)

נכתב 25.06.2026. הכל מוכן בצד הקוד. כאן בדיוק מה לעשות בקונסול, לפי סדר. ~15 דקות.
פרויקט: **siteboss-b2dd2** · קונסול: https://console.firebase.google.com/project/siteboss-b2dd2

> **חשוב — סדר הפעולות:** קודם משימה 1 (אבטחה), ובתוכה: מפעילים ספק → מאמתים את הטלפון שלך באפליקציה → **רק אחר כך** מפרסמים את הכללים המחמירים. אם תפרסם כללים לפני שאימתת — שמירות לענן ייכשלו (יישמרו מקומית ויעלו אחר כך). אני מלווה אותך בכל שלב.

---

## לפני שמתחילים — ה-UID של הבעלים (צריך אותו פעמיים)
זה ה-UID של חשבון הבעלים שיצרת בסבב הקודם (שלב B).
- כתובת: https://console.firebase.google.com/project/siteboss-b2dd2/authentication/users
- בטבלה, בשורת האימייל שלך, העתק את הערך מעמודת **User UID** (מחרוזת ארוכה).
- שמור אותו בצד — תדביק אותו במקום `OWNER_UID_HERE` גם בכללי Firestore וגם בכללי Storage.

---

# משימה 1 (קריטית) — Phone Auth + נעילת נתונים לפי בעלים
**למה:** כרגע נתוני השטח בענן (יומנים, ליקויים, נוכחות+GPS, אירועים, פרטי עובדים זרים) פתוחים לכל אנונימי. זה היחיד שחוסם חיבור לקוח אמיתי.

### 1.1 — הפעל ספק Phone
1. היכנס: https://console.firebase.google.com/project/siteboss-b2dd2/authentication/providers
2. ברשימת הספקים לחץ **Phone** → **Enable** (מתג למעלה) → **Save**.
   *(דורש תוכנית Blaze — כבר מופעלת אצלך. עלות SMS: אגורות בודדות לאימות.)*

### 1.2 — ודא דומיינים מורשים
1. היכנס: https://console.firebase.google.com/project/siteboss-b2dd2/authentication/settings
2. לשונית **Authorized domains** → ודא שמופיעים: `arnonyoram-lang.github.io` וגם `localhost`.
3. אם `arnonyoram-lang.github.io` חסר — **Add domain** → הדבק → **Add**.

### 1.3 — אמת את הטלפון שלך באפליקציה (בדיקת SMS חיה)
1. פתח את האפליקציה: https://arnonyoram-lang.github.io/siteboss/
2. לחץ על ⚙️ (הגדרות) → כפתור **"🔒 אבטח את החשבון בקוד SMS"**.
3. השדה כבר ממולא במספר שלך (מההרשמה). לחץ **שלח קוד**.
4. הזן את 6 הספרות שקיבלת ב-SMS → **אמת**. אמורה להופיע הודעת "אומת ✓".
   - אם כתוב "השירות לא הופעל" — חזור ל-1.1 (הספק לא נשמר).
   - אם ה-SMS לא הגיע — ודא 1.2, ושהמספר תקין בהגדרות.

### 1.4 — אמת שאתה באמת מחובר-בטלפון (לא אנונימי)
1. רענן את האפליקציה (טען מחדש).
2. *(אם נוח: פתח DevTools → Console והקלד `__dkAuth.currentUser.phoneNumber` — אמור להחזיר את המספר שלך ב-+972…, לא null.)*
3. אם זה המספר שלך — מצוין, אתה מחובר-טלפון וזה נשמר לכל הפעלה. המשך ל-1.5.

### 1.5 — פרסם את כללי Firestore המחמירים
1. פתח את הקובץ `firestore.rules` (בתיקיית הפרויקט) — הכל מוכן שם.
2. **החלף את 2 המופעים של `OWNER_UID_HERE` ב-UID שלך** (מהשלב "לפני שמתחילים").
3. היכנס: https://console.firebase.google.com/project/siteboss-b2dd2/firestore/rules
4. סמן הכל (Ctrl+A) → מחק → הדבק את כל תוכן `firestore.rules` → **Publish**.

### 1.6 — בדיקות קבלה (חובה — אל תדלג)
- ✅ באפליקציה שלך: הוסף ליקוי/יומן חדש → ודא שנשמר בלי שגיאה (אתה מחובר-טלפון → נכתב לענן).
- ✅ ב-admin.html (כבעלים): https://arnonyoram-lang.github.io/siteboss/admin.html → אתה רואה את הנתונים.
- ✅ **מבחן הזר:** פתח את כתובת ה-Firestore בגלישה פרטית בלי להתחבר, ונסה לקרוא אוסף `journals` — אמור להיחסם (Permission denied). זה מה שתיקַנו.

### רולבק (אם משהו נשבר)
חזור ל-https://console.firebase.google.com/project/siteboss-b2dd2/firestore/rules והדבק חזרה את כללי **שלב B** (נמצאים ב-`FIREBASE-SECURITY.md`, בלוק "שלב B") → Publish. האפליקציה תחזור להתנהגות הקודמת תוך דקה.

---

# משימה 2 — כללי Firebase Storage (תמונות/חתימות/ת"ז)
**למה:** התמונות והחתימות נשמרות ב-Storage. בלי כללים הן עלולות להיות ציבוריות.

1. פתח את הקובץ `storage.rules` (בתיקיית הפרויקט). **החלף `OWNER_UID_HERE` ב-UID שלך.**
2. היכנס ל-Storage: https://console.firebase.google.com/project/siteboss-b2dd2/storage
   - אם זו פעם ראשונה ומבקש **Get started** — אשר (בחר Production mode אם נשאל; הכללים שלנו יחליפו ממילא).
3. לשונית **Rules** → סמן הכל → מחק → הדבק את תוכן `storage.rules` → **Publish**.
4. בדיקה: באפליקציה צרף תמונה לליקוי ושמור → ב-admin אתה רואה את התמונה. גלישה פרטית לכתובת התמונה בלי התחברות — אמורה להיחסם.

> הכללים האלה נועלים כל מנהל לתיקיית ה-`photos/{הטלפון-שלו}` שלו, והבעלים רואה הכל — בדיוק כמו ב-Firestore.

---

# משימה 3 — הפעלת העוזר החכם (Gemini)
**למה:** אז העוזר ב"🧠 עוזר חכם" יכתוב נהלים/מכתבים/סיכומים אמיתיים במקום "לא מחובר".

1. בקונסול, בתפריט הצד: **Build → AI Logic** (יכול להופיע גם כ-"Firebase AI Logic" / "Gemini in Firebase").
   - קישור ישיר (אם עובד): https://console.firebase.google.com/project/siteboss-b2dd2/ailogic
2. לחץ **Get started / Enable** → כשנשאל איזה ספק, בחר **Gemini Developer API** → אשר.
3. בדיקה: פתח את האפליקציה → 🧠 עוזר חכם → כתוב "תכתוב לי נוהל בטיחות קצר לעבודה בגובה" → אמורה לחזור תשובה אמיתית (בועת "חושב…" ואז טקסט).
   - עלות לפי שימוש, על Blaze. אם תרצה תקרה — נגדיר מכסה.

---

## צ'קליסט סופי
- [ ] 1.1 Phone provider מופעל
- [ ] 1.2 הדומיין מורשה
- [ ] 1.3 אימתתי SMS באפליקציה
- [ ] 1.4 `currentUser.phoneNumber` מציג את המספר שלי
- [ ] 1.5 כללי Firestore (עם ה-UID) פורסמו
- [ ] 1.6 שלושת בדיקות הקבלה עברו (כולל מבחן-הזר)
- [ ] 2 כללי Storage (עם ה-UID) פורסמו
- [ ] 3 AI Logic מופעל והעוזר עונה

**כשתסיים — תגיד לי, ואני אעבור איתך על כל בדיקת קבלה ואוודא שהכל נעול כמו שצריך.**
מה שאני כבר עשיתי בקוד: האפליקציה מעדיפה אוטומטית סשן-טלפון מאומת (ונופלת לאנונימי רק כשאין), כך שברגע שתאמת — הכל "פשוט עובד". הכללים (`firestore.rules` + `storage.rules`) מוכנים ומוקשחים (עברו אודיט אבטחה).
