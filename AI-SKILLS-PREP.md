# הכנה: שכבת מומחי-AI ל"דקה" (SiteBoss)

> מסמך הכנה. כשנגיע להוסיף עוזרי-AI לאפליקציה — נתחיל מכאן. נכתב 17.06.2026.

## המטרה
להביא ל"דקה" את אותה שכבת **מומחי-AI / סקילים** שבנינו ב"עוזר מנהל" (המשרד האישי) —
כל סקיל = יכולת ממוקדת שמפיקה תוצר מוכן-לשימוש (לא תאוריה), מנותבת לפי מה שהמשתמש כותב.

## מקור-אמת לייבוא (אל תכתוב מאפס — תייבא ותתאים)
הקוד הבשל נמצא בריפו **`עוזר מנהל`**:
- `lib/skills.ts` — ה"מוחות" (persona + expertise לכל סקיל).
- `lib/experts.ts` — ניתוב לפי מילות-מפתח (`pickExpert`) + שיוך דיוויזיה.
- עיקרון: **מקור-אמת אחד** לכל סקיל (צ'אט וסוכן חולקים אותו).

⚠️ הבדל סטאק: "דקה" היא HTML SPA (לא Next). הסקילים הם פרומפטים → ניידים לחלוטין.
צריך רק: (א) להעתיק את טקסט ה-persona+expertise; (ב) פונקציית ניתוב פשוטה לפי מילות-מפתח;
(ג) קריאה למודל עם הקשר העסקי/אתר; (ד) להזריק "מוח" (פרטי הפרויקט/הקבלן/האתר) כמו ב"עוזר מנהל".

## הכי רלוונטי למנהל עבודה (להתחיל מאלה)
- **SOP builder (בונה נהלים)** — נוהל בטיחות/עבודה שכל פועל יכול לעקוב אחריו: טריגר, שלבים ממוספרים, בקרה, מקרי-קצה.
- **risk (מפת סיכונים)** — סיכוני אתר: סיכון × סבירות × חומרה + מיתון + 3 הקריטיים. מתחבר ישר לבטיחות.
- **meeting → actions (מהערות למשימות)** — מסיכום פגישת אתר/בטיחות → משימות עם אחראי + יעד + עדיפות.
- **documents (מסמכים רשמיים)** — מכתבים לקבלן/מזמין/רשות, התראות, סיכומי פגישה, דרישות תיקון ליקוי.
- **summary (סיכומים)** — תמצות יומן/שיחה; עוזר ביומן העבודה היומי.
- **premortem (בדיקת כשל מראש)** — לפני שלב/משימה מסוכנת באתר: "נניח שקרתה תאונה/עיכוב — למה?" → מניעה.
- **decision (מטריצת החלטה)** — בחירת קבלן-משנה/ספק/שיטת ביצוע: אפשרויות × קריטריונים → המלצה.

## שאר הסט (לפי הצורך)
inbox-triage · email · cold-outreach · sales follow-up · objection-handler ·
marketing + linkedin-hook + content-multiplier + brand-voice ·
hiring-kit (גיוס פועלים/צוות) · pricing-lab · cashflow · invoices · collections ·
contracts (חוזי קבלן-משנה) · HR/דיני עבודה · privacy · compliance ·
supplier/רכש (חומרי בניין) · service · reputation · weekly-review ·
negotiation (מו"מ מול קבלנים/מזמין) · competitor · one-pager.

## עקרונות יישום (זהים ל"עוזר מנהל")
- מקור-אמת אחד לכל סקיל; תמיד תוצר מוכן-לשימוש, לא הסבר.
- לא להמציא עובדות — `[____]` לפרטים חסרים; בלי מספרים נדיפים מקודדים (להפנות ל"עדכני"/בדיקה מקצועית).
- להזריק את "המוח" (פרטי הפרויקט/האתר/הקבלנים) כדי שהתשובה תתאים לאתר הספציפי.
- ניתוב לפי כוונה — המשתמש כותב חופשי, הסקיל הנכון עונה; לא מכריחים אותו לבחור.
- **אין `prompt-upgrade`** — נוגד את ההבטחה שהמשתמש לא לומד AI.
- mobile-first + דו-לשוני עב/ער (כמו כל "דקה").
